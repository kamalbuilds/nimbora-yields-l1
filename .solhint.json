{
  "extends": "solhint:recommended",
  "plugins": [],
  "rules": {
    "avoid-suicide": "error",
    "avoid-sha3": "warn",
    "function-max-lines": ["warn",120],
    "func-visibility": ["warn", { "ignoreConstructors": true }]
  }
}